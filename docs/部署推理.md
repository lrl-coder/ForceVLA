启动命令：

```
conda activate /root/autodl-tmp/conda_data/envs/forcevla

cd /root/autodl-tmp/ForceVLA

XLA_PYTHON_CLIENT_MEM_FRACTION=0.9 \
python scripts/serve_policy.py \
  --host 0.0.0.0 \
  --port 5555 \
  --checkpoint /root/autodl-tmp/ForceVLA/checkpoints/forcevla_8hz_all_lora/dataset_8hz_all_lora_v1/29999 \
  --config forcevla_8hz_all_lora \
  --device cuda \
  --dtype bfloat16 \
  --warmup 1
```


dry-run：

```
python scripts/serve_policy.py --dry-run --host 127.0.0.1 --port 5555
```

mock:
```
python mock_client.py \
  --host 127.0.0.1 \
  --port 5555 \
  --model forcevla \
  --schema lerobot \
  --dataset flip_box \
  --dataset-index 0 \
  --count 10 \
  --dataset-stride 16 \
  --timeout-ms 120000 \
  --reset-first
```

验证结果：
- 单元测试：13 passed。
- dry-run：ping、reset、真实 ZMQ get_action 全部通过。
- 指定 checkpoint 成功恢复，参数量 3,348,913,940。
- CUDA 加载约 12.8s，首次 JAX 编译/warmup 约 89s。
- warmup 后实测推理约 135ms，客户端往返约 148ms。
- 实际响应为 float32 (16,8)。
